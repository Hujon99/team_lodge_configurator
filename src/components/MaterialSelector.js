import React from 'react';

function MaterialSelector(props) {
    // Logic for material selector

    return (
        <div>
            {/* Material Selector UI */}
        </div>
    );
}

export default MaterialSelector;
